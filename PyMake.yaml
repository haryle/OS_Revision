sum:
  var:
    basic:
      program: null 
      max_thread: 4
      n: 1000
      src: code 
      out: out 
  cmd: 
    - gcc -DMAXTHREAD=$(max_thread) -DN=$(n) -Wall -o $(out)/$(program).o $(src)/$(program).c
    - ./$(out)/$(program).o

clean:
  cmd:
    - rm -rf out/*
    - clear